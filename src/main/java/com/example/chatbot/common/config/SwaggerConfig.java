package com.example.chatbot.common.config;import io.swagger.v3.oas.models.Components;import io.swagger.v3.oas.models.OpenAPI;import io.swagger.v3.oas.models.info.Info;import io.swagger.v3.oas.models.info.Contact;import io.swagger.v3.oas.models.info.License;import io.swagger.v3.oas.models.security.SecurityRequirement;import io.swagger.v3.oas.models.security.SecurityScheme;import io.swagger.v3.oas.models.servers.Server;import org.springdoc.core.models.GroupedOpenApi;import org.springframework.context.annotation.Bean;import org.springframework.context.annotation.Configuration;import java.util.List;/** * swagger 설정 클래스 * * @author : 오승훈 * @fileName : SwaggerConfig * @since : 2026-02-25 */@Configurationpublic class SwaggerConfig {  @Bean  public OpenAPI openAPI() {    // 보안 스키마 정의    String jwtSchemeName = "JWT";    String apiKeySchemeName = "API-Key";    SecurityRequirement securityRequirement = new SecurityRequirement()        .addList(jwtSchemeName)        .addList(apiKeySchemeName);    Components components = new Components()        // JWT 인증        .addSecuritySchemes(jwtSchemeName, new SecurityScheme()            .name("Authorization")            .type(SecurityScheme.Type.HTTP)            .scheme("bearer")            .bearerFormat("JWT")            .in(SecurityScheme.In.HEADER)            .description("JWT 토큰을 입력하세요 (Bearer 제외)")        )        // API Key 인증        .addSecuritySchemes(apiKeySchemeName, new SecurityScheme()            .name("X-API-Key")            .type(SecurityScheme.Type.APIKEY)            .in(SecurityScheme.In.HEADER)            .description("API Key를 입력하세요")        );    return new OpenAPI()        .info(new Info()            .title("Chatbot API")            .description("GPT 기반 AI 챗봇 서비스 API 문서")            .version("v1.0.0")            .contact(new Contact() //연락처                .name("오승훈")                 .email("shoon0728@naver.com")            )            .license(new License()                .name("Apache 2.0")                .url("http://www.apache.org/licenses/LICENSE-2.0.html")            )        )        .servers(List.of(            new Server().url("http://localhost:8080").description("로컬 테스트용")//            ,new Server().url("https://api.chatbot.com").description("운영 서버")        ))//        .addSecurityItem(securityRequirement)        .components(components);  }  @Bean  public GroupedOpenApi authApi() {    return GroupedOpenApi.builder()        .group("\uD83D\uDD11인증")        .pathsToMatch("/api/auth/**")        .build();  }  @Bean  public GroupedOpenApi chatApi() {    return GroupedOpenApi.builder()        .group("\uD83D\uDCDD채팅")        .pathsToMatch("/api/chat/**")        .build();  }}