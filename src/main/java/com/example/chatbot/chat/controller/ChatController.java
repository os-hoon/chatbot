package com.example.chatbot.chat.controller;import com.example.chatbot.chat.dto.ChatRequest;import com.example.chatbot.chat.dto.ChatResponse;import com.example.chatbot.chat.service.ChatService;import lombok.RequiredArgsConstructor;import lombok.extern.slf4j.Slf4j;import org.springframework.http.ResponseEntity;import org.springframework.web.bind.annotation.*;/** * 채팅 API 컨트롤러 * * @author : 오승훈 * @fileName : ChatController * @since : 2026-02-05 */@Slf4j@RestController@RequestMapping("/api/chat")@RequiredArgsConstructorpublic class ChatController {  private final ChatService chatService;  /**   * 채팅 API   *   * @param request 사용자 메시지   * @return AI 응답   */  @PostMapping  public ResponseEntity<ChatResponse> chat(@RequestBody ChatRequest request) {    log.info("채팅 요청 수신 - 메시지: {}", request.getMessage());    // ChatService를 통해 AI 응답 받기    String aiResponse = chatService.chat(request.getMessage());    // ChatResponse로 변환하여 반환    return ResponseEntity.ok(ChatResponse.of(aiResponse));  }}