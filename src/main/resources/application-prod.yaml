spring:  datasource:    url: jdbc:${DATABASE_PUBLIC_URL}?serverTimezone=Asia/Seoul&characterEncoding=UTF-8    username: ${PGUSER}    password: ${PGPASSWORD}    driver-class-name: org.postgresql.Driverserver:  port: ${PORT:8080}openai:  api-key: ${OPENAI_API_SECRET_KEY}  api-url: https://api.openai.com/v1/chat/completions  model: gpt-3.5-turboapi:  key: ${API_KEY}jwt:  secret: ${JWT_KEY}